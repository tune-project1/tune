import Item from "@tune/components/card/index.vue";
import Code from "@tune/components/code/index.vue";
import CodeWrap from "@tune/components/code/wrap.vue";

Once a user's trial ends, its worth taking these steps:
- Allow a trial extension for high quality customers
- Ask for a review to allow trial extensions
- Understand user's stats when their trial, ends

