---
title: 'Nodejs SDK'
slug : 'nodejs-sdk'
description: 'Tune Nodejs SDK'
date: 2025-05-09
---

# Nodejs SDK

## Installation

Use npm to install Tune's nodejs SDK. Run this inside your project:

```bash
npm install --save @tune/sdk
```

## Authorization

Pass your api key like this:

```js
import Tune from "@tune/sdk"
// Or use this if you're on a older version of nodejs
// const Tune = require('@tune/sdk')

const apikey = ''; // <-- Enter your api key here

const ops = new Tune(apikey);
```

## Code examples

Visit the [Playground](/playground) and select Nodejs SDK as the code language to generate code examples.

## For self hosters

Pass your backend's url when initiating the Tune class.

```js
const ops = new Tune(apikey, {
  baseUrl : 'https://xzy.abc.com' // <- Enter your backend's url here.
})
```

You can find this url inside your self hosted instance's profile page under your api key.