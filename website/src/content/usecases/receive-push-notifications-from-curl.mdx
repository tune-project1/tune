---
title: Receive push notifications from cURL
slug: receive-push-notifications-from-curl
description: Learn how to receive and process push notifications using cURL with Tune. Set up your API key and send events easily.
date: 2025-05-22
tags:
  - curl
  - notifications
  - api
icon: 🔔
---

## Why use cURL for push notifications

Receiving push notifications can help you test and debug event streams in your application.

Using cURL gives you a fast way to catch incoming events without writing extra code.

For example, you can verify that your payment service sends notifications correctly by sending them to a local endpoint and inspecting the payload.

## About Tune

Tune is an open source platform to track and manage events across your tech stack.

You can send events, view them in a unified dashboard, and integrate with popular tools.

Integrating with Tune helps you gain real-time insights into your workflows.

![Tune in action](https://tune/images/misc/ops-usage.jpg)

## Setup Tune

1. Visit [app.tune](https://app.tune) and sign up for an account.
2. Create a new project in the dashboard.
3. Copy your API key from the project settings.
4. Keep the key handy for sending events with cURL.

## Sending events with cURL

This example shows how to send a push notification event to Tune using cURL.

```bash
curl https://api.tune/events \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer API_KEY" \
  -d '{
    "name": "push notification received",
    "avatar": "🔔",
    "content": "Received a push notification from Stripe"
  }'
```

This command posts an event named “push notification received” to your Tune project. You can replace the content with your own service name or message.

## Conclusion

Tune saves you time by handling event ingestion and display out of the box.

You can focus on your application while Tune manages the data flow.

Learn more at https://tune and try the [playground](https://tune/playground) to copy and paste snippets instantly.
