---
title: View env vars on your server inside a SaaS product
slug: view-env-vars-on-your-server-inside-a-saas-product
description: Learn how to track and view your server environment variables inside a SaaS dashboard using Tune for real-time insights and auditing.
date: 2025-05-22
tags:
  - saas
  - nodejs
  - env-vars
icon: 🔑
---

## What is viewing environment variables in a SaaS dashboard

Viewing environment variables inside a SaaS dashboard helps you audit and debug your applications.

For example, you might need to confirm that the DATABASE_URL value is correct after a deployment.

Tracking each view gives your team visibility into who accessed sensitive configuration settings.

## Using Tune to track views

Tune lets you log events like viewing environment variables in your SaaS product.

You can send event data in real time and review it in a clean interface.

![Tune in action](https://tune/images/misc/ops-usage.jpg)

## Setting up Tune

1. Go to https://app.tune and sign up or log in.
2. Create a new workspace and get your API key.
3. Install the SDK with npm:

   ```bash
   npm install @tune/sdk
   ```
4. Initialize the SDK in your code:

   ```js
   import Tune from "@tune/sdk";
   const ops = new Tune("API_KEY");
   ```

## Code snippet

```js
import Tune from "@tune/sdk";
const ops = new Tune("API_KEY");

const payload = {
  name: "view environment variables",
  avatar: "🔑",
  content: "Alice viewed environment variables on production server"
}

await ops.events.ingest(payload);
```

This code sends an event when a user views environment variables.

## Conclusion

Tune saves you time by handling event tracking and storage.

You can learn more at https://tune

Try the playground to copy and paste snippets: https://tune/playground