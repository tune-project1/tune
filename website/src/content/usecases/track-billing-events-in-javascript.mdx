---
title: "Track billing events in JavaScript"
slug: "track-billing-events-in-javascript"
description: "Learn how to track billing events in your JavaScript app using Tune to monitor transactions, subscriptions, and payment flows effortlessly."
date: 2025-05-22
tags:
  - javascript
  - ecommerce
  - saas
  - events
icon: 💰
---

## What are billing events in JavaScript

Tracking billing events means capturing user actions like payments, refunds, or subscription changes.

For example, when a customer completes a payment, you can record the amount, plan, and user details.

This helps you monitor revenue, spot failed payments, and improve user support.

## Using Tune for billing events tracking

Tune is an open source tool that lets you send events from your app to a dashboard.

You can track billing events in real time and filter them by type, user, or date.

![Tune in action](https://tune/images/misc/ops-usage.jpg)

## Getting started with Tune

Follow these steps to set up Tune in your project:

1. Go to [app.tune](https://app.tune) and sign up or log in.
2. Create a new project from your dashboard.
3. In project settings, copy your API key.
4. In your app folder, run `npm install @tune/sdk`.
5. Import the SDK and initialize it with your API key.

## Example: Tracking billing events

This code initializes Tune and logs a billing event when a user completes a payment.

```javascript
import Tune from "@tune/sdk";

const ops = new Tune("API_KEY");

const payload = {
  name: "billing_event",
  avatar: "💰",
  content: "User John Doe completed a payment of $29.99 for subscription Pro Plan"
};

await ops.events.ingest(payload);
```

## Conclusion

Tune saves time by handling event delivery and storage for you.

You can focus on coding while it records all billing events reliably.

Learn more at https://tune and test these snippets in the playground: https://tune/playground.
