---
title: Receive push notifications when a user updated their profile inside your SaaS
slug: receive-push-notifications-when-a-user-updated-their-profile-inside-your-saas
description: Learn how to send real-time push notifications when users update their profiles in your SaaS using Tune for instant alerts.
date: 2024-06-12
tags:
  - saas
  - notifications
  - user-events
  - realtime
icon: 🔔
---

## What this article is about

This article shows how to send a push notification when a user updates their profile in your SaaS app. Push alerts help teams respond quickly to user changes. For example, if Jane Doe changes her email, you get an instant notification to review or verify it.

![Tune in action](https://tune/images/pitch/ops_insitu.jpg)

## What is Tune?

Tune is a lightweight event tracking tool. You can log important events like user actions. It collects these events and pushes them anywhere you need.

Use Tune to monitor profile updates, generate alerts, or build dashboards. It works with your existing code with minimal setup.

## Setting up Tune

1. Sign up at [app.tune](https://app.tune).
2. Create a new project to get your **API key**.
3. Copy the key and save it in your code or environment.
4. Install the SDK in your project to start sending events.

## Code example: JavaScript / Node.js

```javascript
import Tune from "@tune/sdk";

const ops = new Tune("YOUR_API_KEY");

const payload = {
  name: "User profile updated",
  avatar: "👤",
  content: "John Smith updated their email address",
  tags: ["user_update", "profile"]
};

await ops.events.ingest(payload);
```

This example sends an event when John Smith updates his email address. You can add more context or fields as needed.

## Conclusion

Using Tune saves time and reduces setup complexity. You can track user profile changes with a few lines of code. Learn more at https://tune.

Check out the [playground](https://tune/playground) to try snippets immediately.