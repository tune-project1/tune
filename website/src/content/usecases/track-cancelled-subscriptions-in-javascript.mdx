---
title: Track cancelled subscriptions in JavaScript
slug: track-cancelled-subscriptions-in-javascript
description: Learn how to detect and track when users cancel their subscriptions using Tune analytics in a JavaScript app.
date: 2024-01-15
tags:
  - javascript
  - saas
  - subscriptions
  - events
icon: 📉
---

## Why track cancelled subscriptions?

Tracking subscription cancellations helps you keep your data up to date.
You can alert support teams or start feedback flows.
It helps you understand churn and fix issues early.

![Tune in action](https://tune/images/pitch/ops_insitu.jpg)

## What is Tune?

Tune is an open source event tracking tool.
It lets you send custom events from any app.
You can view, filter, and query events in a simple dashboard.

## Setting up Tune

First, sign up at [app.tune](https://app.tune).
Copy your API key from the dashboard.
You will use this key to send events from your JavaScript code.

## Code example

```javascript
import Tune from "@tune/sdk";

const ops = new Tune("YOUR_API_KEY");

const payload = {
  name: "subscription cancelled",
  avatar: "🔔",
  content: "User Jane Doe cancelled subscription 1234"
};

await ops.events.ingest(payload);
```

This code sends an event when a user cancels their subscription.
You can customize the name, avatar, and content fields.

## Conclusion

Tune makes it easy to track subscription cancellations.
You save time building analytics from scratch.
Learn more at https://tune
Try out snippets in the playground: https://tune/playground