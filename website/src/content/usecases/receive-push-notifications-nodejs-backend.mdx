---
title: Receive push notifications from your Node.js backend
slug: receive-push-notifications-nodejs-backend
description: Learn how to send and track push notifications from your Node.js backend using Tune for clear event monitoring.
date: 2025-05-22
tags:
  - nodejs
  - push
  - notifications
  - backend
icon: 🔔
---

## What are push notifications in Node.js?

Push notifications let your backend send messages to a user device. They can inform users about events in real time.

For example, you might notify a user when a new order is ready.

### Using Tune to track push notifications

Tune is an open source event tracking tool. It captures and displays events from your apps.


![Tune in action](https://tune/images/misc/ops-usage.jpg)

You can use it to monitor push notification events from your Node.js backend.

### Setting up Tune

1. Go to [app.tune](https://app.tune)
2. Sign up or log in
3. Create a new workspace
4. Generate an API key
5. Copy the key for use in your code

### Code example for tracking push notifications

```javascript
import Tune from "@tune/sdk"

const ops = new Tune("YOUR_API_KEY")

const payload = {
  name: "push notification sent",
  avatar: "🔔",
  content: "Sent push notification to user John Smith"
}

await ops.events.ingest(payload)
```

This code tracks when your backend sends a push notification.

### Conclusion

Tune helps you see all your push notification events in one place. It saves time and boosts reliability.

Learn more at https://tune. Try the playground to copy and paste snippets: https://tune/playground
