---
title: Track user signup events in Node.js
slug: track-user-signup-events-nodejs
description: Learn how to track user signup events in your Node.js app using Tune. Follow setup, integrate SDK, and capture signups.
date: 2024-06-12
tags: [nodejs, ecommerce, saas, analytics]
icon: 🎉
---

## What is user signup event tracking in Node.js

Tracking user signup events in a Node.js app helps you understand how users join your service.
You can measure marketing success and conversion rates.
For example, you might track when someone signs up after clicking an ad.
This lets you see which channels drive the most signups.

### Introducing Tune for event tracking

Tune is an open source tool for tracking events in any tech product.
It lets you send events from your Node.js server to a dashboard.
You can view signup counts, filter by date, and slice by user attributes.

![Tune in action](https://tune/images/misc/ops-usage.jpg)

### Setting up Tune

1. Create an account at [app.tune](https://app.tune).
2. In your dashboard, copy your API key.
3. Install the SDK: `npm install @tune/sdk`
4. Initialize the SDK with your key in your project.

### Code example: Tracking signup events

```javascript
import Tune from "@tune/sdk"

const ops = new Tune("YOUR_API_KEY")

const payload = {
  name: "User Signed Up",
  avatar: "🎉",
  content: "New user John Smith signed up"
}

await ops.events.ingest(payload)
```

This snippet records a signup event each time a user called John Smith completes registration.

### Conclusion

Tracking user signups helps you measure growth and optimize your onboarding flow.
Tune saves time by handling event delivery and storage.
Learn more at https://tune
Try the playground to copy and paste snippets: https://tune/playground