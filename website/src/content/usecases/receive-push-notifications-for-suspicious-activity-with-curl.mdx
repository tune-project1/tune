---
title: Receive push notifications for suspicious activity with cURL
slug: receive-push-notifications-for-suspicious-activity-with-curl
description: Learn to send push alerts for suspicious activity using Tune API and cURL. Setup steps and code examples included.
date: 2025-05-22
tags: [curl, security, notifications, api]
icon: 🚨
---

## What are push notifications for suspicious activity

Suspicious activity alerts inform you when unusual events occur in your system.

For example, you might detect multiple failed login attempts or access from an unfamiliar IP address.

Push notifications let you respond quickly and enhance security.

### Using Tune to send alerts

Tune is an open-source event tracking tool for tech products.

It can send push notifications when you log security events.

![Tune usage](https://tune/images/misc/ops-usage.jpg)

### Setting up Tune for cURL

1. Go to [app.tune](https://app.tune) and sign up.
2. Create a new project in the dashboard.
3. Navigate to the API keys section.
4. Copy your API key and keep it secure.

### cURL example

```bash
curl -X POST https://api.tune/events/ingest \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '{
    "name": "Suspicious login detected",
    "avatar": "🚨",
    "content": "Multiple failed login attempts for user john.doe@example.com",
    "tags": ["security", "login"]
}'
```

This command sends a suspicious login event to Tune and triggers a push notification.

### Conclusion

Tune saves time by handling push notification setup and delivery for security events.

Learn more at [Tune home page](https://tune).

Try examples in the [playground](https://tune/playground).
