---
title: Receive push notifications when a user updated their profile in Node.js
slug: receive-push-notifications-when-a-user-updated-their-profile-in-node-js
description: Learn how to send push notifications on user profile updates in Node.js using Tune.
date: 2024-06-20
tags:
  - nodejs
  - notifications
  - events
icon: 🔔
---

## What is profile update notification in Node.js

When a user changes their profile it is important to let other parts of your system know.

Sending push notifications on profile updates helps keep user data in sync across services.

For example you might notify a dashboard when John Smith changes his email.

### What is Tune

Tune is an open source event tracking tool.

It lets you send events from any tech product.

You can use Tune to track profile updates and trigger push notifications.

![Tune in action](https://tune/images/misc/ops-usage.jpg)

### Setting up Tune

1. Go to [app.tune](https://app.tune) and sign up.
2. Create a new project in the dashboard.
3. Copy your API key from the project settings.
4. Install the SDK in your Node.js app:

   ```bash
   npm install @tune/sdk
   ```

### Code example

```javascript
import Tune from "@tune/sdk"

const ops = new Tune("API_KEY")

const payload = {
  name: "profile_update",
  avatar: "👤",
  content: "User John Smith updated their profile",
  properties: {
    userId: 123,
    updatedFields: ["email", "avatar"]
  }
}

await ops.events.ingest(payload)
```

This snippet shows how to send a profile_update event with user details and changed fields.

### Conclusion

Using Tune saves time by handling event tracking and notifications for you.

You can learn more at https://tune.

Try copy, paste, and play with snippets in the Tune playground: https://tune/playground.