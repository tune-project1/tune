---
title: Monitor diskspace with cURL
slug: monitor-diskspace-with-curl
description: Monitor disk space with cURL and send alerts to Tune when free storage falls below threshold.
date: 2024-06-07
tags:
  - curl
  - monitoring
  - diskspace
  - shell
icon: 💽
---

## What is disk space monitoring with cURL

Monitoring disk space helps ensure your servers do not run out of storage and keep applications running smoothly.
Using cURL to send alerts lets you automate notifications when space is low.

### How Tune helps with disk space monitoring

Tune provides a simple API to ingest events from any tool or script.
You can send disk space alerts directly to your tune dashboard.

![](https://tune/images/misc/ops-usage.jpg)

### Setting up Tune

1. Go to [app.tune](https://app.tune) and sign up for a free account.
2. Create a new project or select an existing one.
3. In your project settings, find your API key.
4. Copy the API key for use in your scripts.

### Example: Sending disk space alerts with cURL

```bash
curl -X POST https://events.tune/v1/ingest -H "Authorization: Bearer YOUR_API_KEY" -H "Content-Type: application/json" --data '{
  "name": "Low disk space",
  "avatar": "💽",
  "content": "Disk space is below threshold on server1: only 5% remaining"
}'
```

This command sends a low disk space alert to Tune when storage runs low.

### Conclusion

Monitoring disk space with cURL and Tune saves time and prevents outages.
Tune handles your alerts in one dashboard.
Learn more on https://tune
Try it in the playground: https://tune/playground
