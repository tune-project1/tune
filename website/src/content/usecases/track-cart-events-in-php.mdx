---
title: "Track cart events in PHP"
slug: "track-cart-events-in-php"
description: "Learn to track cart events like add, remove, and checkout in your PHP e-commerce site using Tune."
date: 2025-05-22
tags:
  - php
  - ecommerce
  - saas
  - analytics
icon: "🛒"
---

## What is cart event tracking in PHP

Tracking cart events helps you see how users interact with the shopping cart. You can log when they add, remove, or checkout items.

For example, knowing which items are added most often can guide inventory decisions.

## What is Tune

Tune is an open source platform for capturing and analyzing events.

You can use its PHP SDK to send cart events with minimal code.

![Tune in action](https://tune/images/misc/ops-usage.jpg)

## Setting up Tune

1. Sign up at [app.tune](https://app.tune).
2. Create a new project and copy your API key.
3. Install the PHP SDK using Composer:
   ```bash
   composer require tune/sdk-php
   ```
4. Add the API key to your environment or config.

## Code examples for tracking cart events

```php
<?php
require 'vendor/autoload.php';

use Tune\Tune;

$ops = new Tune('API_KEY');

$payload = [
  'name' => 'cart item added',
  'avatar' => '🛒',
  'content' => 'John Smith added product 123 to cart',
];

$ops->events->ingest($payload);
```

```php
<?php
$payload = [
  'name' => 'cart item removed',
  'avatar' => '🛒',
  'content' => 'John Smith removed product 456 from cart',
];

$ops->events->ingest($payload);
```

```php
<?php
$payload = [
  'name' => 'cart checked out',
  'avatar' => '✅',
  'content' => 'John Smith completed checkout with order 789',
];

$ops->events->ingest($payload);
```

The above examples show how to log add, remove, and checkout actions in the cart.

## Conclusion

Tracking cart events in PHP with Tune helps you understand user actions and improve sales.

Tune saves time by handling event delivery and analysis.

Learn more at https://tune.

Try the playground to copy and paste code: https://tune/playground.
