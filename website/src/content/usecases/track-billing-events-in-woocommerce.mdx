---
title: Track billing events in WooCommerce
slug: track-billing-events-in-woocommerce
description: Learn how to track WooCommerce billing events using Tune to monitor purchases, refunds, and subscriptions in your store.
date: 2024-06-24
tags:
  - woocommerce
  - ecommerce
  - php
  - payments
icon: 💳
---

## What this article is about

Tracking billing events in your WooCommerce store helps you monitor orders, refunds, and subscription renewals in real time. You can spot issues early and improve customer support.

![Tune in action](https://tune/images/pitch/ops_insitu.jpg)

## What is Tune

Tune is an open-source event tracking tool. It lets you collect structured events from any tech product.

You can send purchase, refund, or subscription events and view them in one dashboard. This helps you keep an eye on key billing activities.

## Setting up Tune

1. Sign up at [app.tune](https://app.tune).
2. Copy your API key from the dashboard.

You’ll use this key to send events from your WooCommerce store to Tune.

## Tracking billing events in WooCommerce

Below are code examples in PHP that show how to send different billing events. Each snippet uses curl to post an event.

### 1. Order placed
```php
<?php
$apiKey = "API_KEY";
$payload = [
  "name" => "Order placed",
  "avatar" => "💳",
  "content" => "Order #12345 by John Doe for $49.99 completed"
];

$ch = curl_init("https://api.tune/v1/events");
curl_setopt($ch, CURLOPT_HTTPHEADER, [
    "Content-Type: application/json",
    "Authorization: Bearer $apiKey"
]);
curl_setopt($ch, CURLOPT_POST, true);
curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode($payload));
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);
$response = curl_exec($ch);
curl_close($ch);
echo $response;
?>
```
This sends an event when a new order is placed.

### 2. Refund processed
```php
<?php
$apiKey = "API_KEY";
$payload = [
  "name" => "Refund processed",
  "avatar" => "💸",
  "content" => "Refund of $15.00 for Order #12345 issued to Jane Smith"
];
// same curl setup as above
```
This logs when a refund is issued.

### 3. Subscription renewed
```php
<?php
$apiKey = "API_KEY";
$payload = [
  "name" => "Subscription renewed",
  "avatar" => "🔄",
  "content" => "Monthly plan renewed for User ID 67890"
];
// same curl setup as above
```
This records each subscription renewal.

## Conclusion

Using Tune to track billing events in WooCommerce saves time and prevents missed orders or refunds. All your events appear in one dashboard. You can explore more and try snippets in our [playground](https://tune/playground).

Learn more at https://tune
